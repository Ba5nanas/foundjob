# FoundJob Landingpage

Public job board, job seeker dashboard, and company dashboard.

Rules:

- Use explicit domain API routes only.
- Provider modules call Main Control through Unix Domain Socket.
- Never call File Service directly.
- Never resolve `fileId` in SSR.
