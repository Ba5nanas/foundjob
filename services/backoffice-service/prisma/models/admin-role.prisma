model AdminRole {
  id          String   @id @default(cuid())
  name        String   @unique
  level       Int
  permissions String[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([level])
}
