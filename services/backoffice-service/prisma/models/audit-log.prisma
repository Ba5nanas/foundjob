model AuditLog {
  id          String   @id @default(cuid())
  actorUserId String
  action      String
  resource    String
  resourceId  String?
  metadata    Json?
  createdAt   DateTime @default(now())

  @@index([actorUserId])
  @@index([resource, resourceId])
}
